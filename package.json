{
  "name": "onethu",
  "private": true,
  "version": "0.7.11",
  "description": "OneTHU — 清华万物，汇合于一体",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @onethu/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @onethu/core test",
    "lint:ui-copy": "node tools/ui-copy-lint.mjs",
    "lint:docs": "node tools/docs-prose-lint.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
