<?xml version="1.0" encoding="utf-8"?>
<!--
  宿主小组件「方块 2×2」的元信息（初始占位 2 列 × 2 行）。

  - 与标准形态共用同一套布局（@layout/onethu_widget）：行数由实际高度决定，故这里只需要
    声明初始占位与可拖动的下界；放置后用户仍可自由改尺寸。
  - minResizeWidth/Height 统一 110×40dp：放开到「2 列 × 1 行」以便拖成窄条。
  - previewImage：部分国产启动器（ColorOS 等）不认 previewLayout，选择器里会显示空白
    甚至放置失败 → 同时提供静态 PNG 兜底（R21c 用户实录：ColorOS 上放上去桌面没有）。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:updatePeriodMillis="1800000"
    android:previewLayout="@layout/onethu_widget_preview_grid"

    android:previewImage="@drawable/onethu_widget_preview_grid"
    android:widgetFeatures="reconfigurable"
    android:initialLayout="@layout/onethu_widget"
    android:configure="app.onethu.mobile.OnethuWidgetConfigActivity"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/onethu_widget_desc" />
