# 寻迹：高德 Web 服务 Key（复制本文件为 .env 后填入真实值；.env 已被 gitignore）
# 申请：console.amap.com → 应用管理 → 创建应用 → 添加 Key（服务平台选「Web 服务」，32 位十六进制）
# 生效：填好后重启构建（pnpm tauri dev / cargo build），编译期注入进二进制（XOR 混淆）
TRACE_AMAP_KEY=0000000000000000000000000000dead
