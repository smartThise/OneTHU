# exFAT 移动盘会为每个文件生成 AppleDouble 元数据（._*），
# tauri 构建脚本 glob 权限文件时会误读导致崩溃。
# 本机构建请导出环境变量（CI 不受影响，走默认 target 目录）：
#   export CARGO_TARGET_DIR=/Users/st/.cache/onethu/cargo-target
# （不再写死 [build] target-dir —— 之前导致 CI runner Permission denied）
